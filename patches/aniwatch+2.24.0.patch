diff --git a/node_modules/aniwatch/dist/index.d.ts b/node_modules/aniwatch/dist/index.d.ts
index a044c06..07f1539 100644
--- a/node_modules/aniwatch/dist/index.d.ts
+++ b/node_modules/aniwatch/dist/index.d.ts
@@ -178,6 +178,7 @@ type ScrapedHomePage = Pick<ScrapedAnimeCategory, "genres" | "top10Animes"> & {
     mostPopularAnimes: MostPopularAnime[];
     mostFavoriteAnimes: MostFavoriteAnime[];
     latestCompletedAnimes: LatestCompletedAnime[];
+    newAnimes: LatestEpisodeAnime[];
 };
 
 type EstimatedSchedule = {
diff --git a/node_modules/aniwatch/dist/index.js b/node_modules/aniwatch/dist/index.js
index 77d528a..86ccc44 100644
--- a/node_modules/aniwatch/dist/index.js
+++ b/node_modules/aniwatch/dist/index.js
@@ -348,6 +348,39 @@ var extractMostPopularAnimes = ($, selector, scraperName) => {
     throw HiAnimeError.wrapError(err, scraperName);
   }
 };
+var extractNewAnimes = ($, scraperName) => {
+  try {
+    const animes = [];
+    const newAnimesSection = $('h2.cat-heading:contains("New On HiAnime")').closest('.block_area');
+    if (newAnimesSection.length === 0) {
+      return animes;
+    }
+    const selector = '.film_list-wrap .flw-item';
+    newAnimesSection.find(selector).each((_, el) => {
+      const animeId = $(el).find(".film-detail .film-name .dynamic-name")?.attr("href")?.slice(1).split("?ref=search")[0] || null;
+      animes.push({
+        id: animeId,
+        name: $(el).find(".film-detail .film-name .dynamic-name")?.text()?.trim(),
+        jname: $(el).find(".film-detail .film-name .dynamic-name")?.attr("data-jname")?.trim() || null,
+        poster: $(el).find(".film-poster .film-poster-img")?.attr("data-src")?.trim() || null,
+        duration: $(el).find(".film-detail .fd-infor .fdi-item.fdi-duration")?.text()?.trim(),
+        type: $(el).find(".film-detail .fd-infor .fdi-item:nth-of-type(1)")?.text()?.trim(),
+        rating: $(el).find(".film-poster .tick-rate")?.text()?.trim() || null,
+        episodes: {
+          sub: Number(
+            $(el).find(".film-poster .tick-sub")?.text()?.trim().split(" ").pop()
+          ) || null,
+          dub: Number(
+            $(el).find(".film-poster .tick-dub")?.text()?.trim().split(" ").pop()
+          ) || null
+        }
+      });
+    });
+    return animes;
+  } catch (err) {
+    throw HiAnimeError.wrapError(err, scraperName);
+  }
+};
 function retrieveServerId($, index, category) {
   return $(
     `.ps_-block.ps_-block-sub.servers-${category} > .ps__-list .server-item`
@@ -439,6 +472,7 @@ async function getHomePage() {
     mostPopularAnimes: [],
     mostFavoriteAnimes: [],
     latestCompletedAnimes: [],
+    newAnimes: [],
     genres: []
   };
   try {
@@ -551,6 +585,7 @@ async function getHomePage() {
       "#anime-featured .row div:nth-of-type(4) .anif-block-ul ul li",
       getHomePage.name
     );
+    res.newAnimes = extractNewAnimes($, getHomePage.name);
     return res;
   } catch (err) {
     throw HiAnimeError.wrapError(err, getHomePage.name);
